#include <stdio.h>

int main() {
    // Your code here
printf("current Time %s\n", __TIME__); // returns the current time
printf("current Date %s\n", __DATE__); // returns the current date
printf("current File %s\n", __FILE__); // returns the current file
    return 0;
}